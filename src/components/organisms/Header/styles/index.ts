import styled from 'styled-components';
import { Color } from '../../../../variables/colors';

const { primaryDarker } = Color;

export const Container = styled.div`
  background-color: ${primaryDarker};
`;
